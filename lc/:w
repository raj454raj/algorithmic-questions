#include<bits/stdc++.h>
#define g getchar_unlocked
#define ll long long int
using namespace std;
int readnum() {
    int n = 0;
    char c = g();
    while(c < '0' || c > '9') c = g();
    while(c >= '0' && c <= '9') n = 10 * n + c - '0', c = g();
    return n;
}
int findFirst(vector<int> v, int x) {
    int mid, beg = 0, end = v.size() - 1;
    int prev = -1;
    while(beg <= end) {
        mid = end + (beg - end) / 2;
        if(a[mid] < x)
            beg = mid + 1;
        else if(a[mid] > x && prev != -1)
            return prev;
        else if(a[mid] > x)
            end = mid - 1;
        else {
            prev = mid;
            end = mid - 1;
        }
    }

}

int main() {
    int t;
    t = readnum();
    while(t--) {
    }
    return 0;
}

