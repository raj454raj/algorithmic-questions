#include<bits/stdc++.h>
using namespace std;
vector< vector<int> > combine(int n, int k) {
    vector<int> v;
    vector<int> tmp;
    for(int i = 1 ; i <= n ; i++) 
        v.push_back(i);
    for(int i = 1 ; i < n ; i++) {
        for(int j = i + 1 ; j <= n ; j++) {
            
        }
    }
}

int main() {
    int n, k;
    cin >> n >> k;
    return 0;
}
